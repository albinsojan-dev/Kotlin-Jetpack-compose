### Add Text
````
    Column{
        Text(
            text = stringResource(R.string.name),
            fontSize = 45.sp,
            color = Color.Green,
            modifier = Modifier
                .fillMaxSize()
                .wrapContentSize(Alignment.Center)

        )
    }
}

````
###
